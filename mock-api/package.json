{
  "name": "flutter-fintech-mock-api",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "start:json-only": "json-server --watch db.json --routes routes.json --port 3000 --host 0.0.0.0"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
